# Create temporary directory
mkdir ~/temporary
cd ~/temporary

# Installing GitHub
echo "Installing GitHub"
curl -L -o gitHub.zip "https://central.github.com/mac/latest"
unzip gitHub.zip
mv GitHub.app /Applications
rm gitHub.zip

# Deleting temporary directory
rm -r ~/temporary
